{
  "name": "fcc-broadband-provider-map",
  "version": "1.0.0",
  "private": true,
  "description": "Provider search + FCC-style hex coverage map with Census region analytics",
  "type": "module",
  "scripts": {
    "build": "npm run build:data",
    "start": "node server.js",
    "build:metrics": "node scripts/build-region-metrics.mjs",
    "build:data": "npm run build:metrics"
  }
}
